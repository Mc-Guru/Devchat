{
  "name": "moonchat",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npm install --save express socket.io",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "mcguru",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
